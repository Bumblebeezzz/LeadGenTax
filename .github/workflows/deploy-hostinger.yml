name: Deploy to Hostinger VPS (FTP)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.HOSTINGER_FTP_HOST }}
          username: ${{ secrets.HOSTINGER_FTP_USER }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          server-dir: /var/www/leadgentax.au/
          dangerous-clean-slate: false
          log-level: verbose
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.DS_Store
            **/install.sh
            **/deploy-auto.sh
            **/*.md
            **/render.yaml
            **/Dockerfile
            **/.dockerignore
            **/README.md
            **/DEPLOYMENT_NOTES.md
            **/HOSTINGER_INSTALLATION_GUIDE.md
            **/INSTRUCTIONS_RENDER.md
            **/README_RENDER.md
            **/QUICK_START.md
            **/render.yaml
            **/Dockerfile
            **/.dockerignore
            **/composer.json
            **/composer.lock
            **/.htaccess.backup
            **/Hand Shake.png
            **/Men Smiling*.png
            **/LeadGenTax.mov
            **/LeadGenTax.png
          dangerous-clean-slate: false

